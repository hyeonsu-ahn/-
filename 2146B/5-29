#2146B


t = int(input())

for _ in range(t) :
  n,m = map(int, input().split())
  result = 'YES'
  total_element = set()
  set_li = []
  perfect_set = set()
  for i in range(1,m+1) :
    perfect_set.add(i)
 
  for i in range(n) :
    #중복제거한 집합.
    st = set(list(map(int, input().split())))
    total_element |= st
    set_li.append(st)
  
  

  if (perfect_set - total_element) != set() :
    result = 'NO'
    
  else:
    temp_set = set()
    rare_set = set_li[0]
    #가장희귀한 집합 고르기.
    for i in range(1,len(set_li)) :
      israre_set = perfect_set - temp_set
      #희귀하면 더 길이가 작아 
      if len(israre_set - set_li[i]) < len(israre_set - rare_set)  :
        rare_set = set_li[i]
    temp_set |= rare_set


    for i in range(len(set_li)) :
      target_set = set_li[i]
      #해당 집합에서 겹치는게 없다.
      if ((perfect_set - temp_set) - target_set)== (perfect_set - temp_set) :
        set_li[i] = {0}
    set_li.remove({0})

    rare_set = set_li[0]
    for i in range(1,len(set_li)) :
      israre_set = perfect_set - temp_set
      #희귀하면 더 길이가 작아 
      if len(israre_set - set_li[i]) < len(israre_set - rare_set)  :
        rare_set = set_li[i]
    temp_set |= rare_set

    for i in range(len(set_li)) :
      target_set = set_li[i]
      #해당 집합에서 겹치는게 없다.
      if ((perfect_set - temp_set) - target_set)== (perfect_set - temp_set) :
        set_li[i] = {0}
    set_li.remove({0})

    rare_set = set_li[0]
    for i in range(1,len(set_li)) :
      israre_set = perfect_set - temp_set
      #희귀하면 더 길이가 작아 
      if len(israre_set - set_li[i]) < len(israre_set - rare_set)  :
        rare_set = set_li[i]
    temp_set |= rare_set

    if (perfect_set - temp_set) != set() :
      result = 'NO'


  print(result)


---

1. 수정한 것
  가장 희귀한 것을 뽑는것으로 변경. 기존에는 가장 길이가 긴 set을 뽑았는데 그랬더니 오답이 나옴. 

2. 수정해야하는것
  문제의 마지막 테스트케이스가 정답이 "NO"인데 왜 "NO"일까?
  문제를 잘못이해한것같다.









