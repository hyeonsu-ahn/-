#2146B


t = int(input())

for _ in range(t) :
  n,m = map(int, input().split())
  result = 'YES'
  total_element = set()
  set_li = []
  perfect_set = set()
  for i in range(1,m+1) :
    perfect_set.add(i)

  for i in range(n) :
    #중복제거한 집합.
    st = list(map(int, input().split()))
    st.pop(0)
    total_element |= set(st)
    set_li.append(st)
  total_element = list(total_element)


  if (perfect_set - total_element) != set() :
    result = 'NO'
  else:
    


  print(result)






1. 수정한것
  집합을 set으로 안하고 list로 설정했다.
2. 바꿔야하는것
  가장 희귀한 집합을 고르는 알고리즘을 생각해야한다. 기존것은 삭제했다.
