#2146B


t = int(input())

for _ in range(t) :
  n,m = map(int, input().split())
  result = 'YES'
  total_element = set()
  set_li = []
  perfect_set = set()
  for i in range(1,m+1) :
    perfect_set.add(i)

  for i in range(n) :
    #중복제거한 집합.
    st = list(map(int, input().split()))
    st.pop(0)
    st = set(st
             )
    total_element |= st
    set_li.append(st)



  if (perfect_set - total_element) != set() :
    result = 'NO'

  else:
    temp_set = set()
    rare_set = set_li[0]
    #가장희귀한 집합 고르기.
    for i in range(1,len(set_li)) :
      israre_set = (perfect_set - temp_set)& set_li[i]
      tem_r_set = (perfect_set - temp_set)&rare_set
      for j in range(len(set_li)) :
        if set_li[i] == set_li[j] and set_li[j] == rare_set:
          continue
        elif set_li[i] == set_li[j] :
          tem_r_set -= set_li[j]
          continue
        elif set_li[j] == rare_set :
          israre_set -= set_li[j]
          continue
        israre_set -= set_li[j]
        tem_r_set -= set_li[j]
      
      #희귀하면 더 길이가 커
      if len(israre_set) > len(tem_r_set)  :
        rare_set = set_li[i]
    temp_set |= rare_set


    for i in range(len(set_li)) :
      target_set = set_li[i]
      #해당 집합에서 겹치는게 없다.
      if ((perfect_set - temp_set) - target_set)== (perfect_set - temp_set) :
        set_li[i] = {0}
    set_li.remove({0})

    rare_set = set_li[0]
    #가장희귀한 집합 고르기.
    for i in range(1,len(set_li)) :
      israre_set = (perfect_set - temp_set)& set_li[i]
      tem_r_set = (perfect_set - temp_set)&rare_set
      for j in range(len(set_li)) :
        if set_li[i] == set_li[j] and set_li[j] == rare_set:
          continue
        elif set_li[i] == set_li[j] :
          tem_r_set -= set_li[j]
          continue
        elif set_li[j] == rare_set :
          israre_set -= set_li[j]
          continue
        israre_set -= set_li[j]
        tem_r_set -= set_li[j]
      
      #희귀하면 더 길이가 커
      if len(israre_set) > len(tem_r_set)  :
        rare_set = set_li[i]
    temp_set |= rare_set


    for i in range(len(set_li)) :
      target_set = set_li[i]
      #해당 집합에서 겹치는게 없다.
      if ((perfect_set - temp_set) - target_set)== (perfect_set - temp_set) :
        set_li[i] = {0}
    set_li.remove({0})

    rare_set = set_li[0]
    #가장희귀한 집합 고르기.
    for i in range(1,len(set_li)) :
      israre_set = (perfect_set - temp_set)& set_li[i]
      tem_r_set = (perfect_set - temp_set)&rare_set
      for j in range(len(set_li)) :
        if set_li[i] == set_li[j] and set_li[j] == rare_set:
          continue
        elif set_li[i] == set_li[j] :
          tem_r_set -= set_li[j]
          continue
        elif set_li[j] == rare_set :
          israre_set -= set_li[j]
          continue
        israre_set -= set_li[j]
        tem_r_set -= set_li[j]
      
      #희귀하면 더 길이가 커
      if len(israre_set) > len(tem_r_set)  :
        rare_set = set_li[i]
    temp_set |= rare_set


    if (perfect_set - temp_set) != set() :
      result = 'NO'


  print(result)



---

1. 수정한 것.
  희귀한 집합을 고르는 기준을 바꿨다. 앞으로 채워야 할 원소들을 많이 가지고있고 다른 집합들이 더 적게 가지고있는 집합을 희귀한 집합으로 잡았다.

2. 수정해야할것.
  그럼에도 4번째 케이스가 잘못되는데 이건 추적을 해봐야할것같다. 그리고 희귀한 집합 고르는 로직도 보기 편하게 수정하면 좋을 것 같다. 지금은 너무 보기 불편하다.
